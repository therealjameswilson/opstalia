{
  "name": "opstalia",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "OPSTALIA 1.0 — an independent, unclassified declassified-records search engine for official U.S. Government repositories.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.app.json && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.app.json --noEmit && tsc -p tsconfig.worker.json --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "npm run build && playwright test",
    "test:security": "vitest run tests/security",
    "test:a11y": "npm run build && playwright test tests/e2e/accessibility.spec.ts",
    "check": "npm run lint && npm run typecheck && npm run test && npm run build",
    "worker:dev": "wrangler dev --config worker/wrangler.toml",
    "worker:deploy": "wrangler deploy --config worker/wrangler.toml",
    "worker:secret": "wrangler secret put NARA_API_KEY --config worker/wrangler.toml",
    "audit": "npm audit --audit-level=high",
    "secret:scan": "node scripts/secret-scan.mjs",
    "indexes:frus": "node scripts/build-frus-index.mjs",
    "indexes:iscap": "node scripts/build-iscap-index.mjs",
    "indexes:ndc": "node scripts/build-ndc-index.mjs",
    "indexes:refresh": "npm run indexes:frus && npm run indexes:iscap && npm run indexes:ndc"
  },
  "dependencies": {
    "idb": "8.0.3",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260729.1",
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.0",
    "@testing-library/jest-dom": "7.0.0",
    "@testing-library/react": "16.3.2",
    "@types/node": "26.1.2",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "@vitejs/plugin-react": "6.0.4",
    "axe-core": "4.12.1",
    "eslint": "10.8.0",
    "globals": "17.8.0",
    "jsdom": "30.0.1",
    "typescript": "7.0.2",
    "typescript-eslint": "8.65.0",
    "vite": "8.1.5",
    "vitest": "4.1.10",
    "wrangler": "4.115.0"
  },
  "engines": {
    "node": ">=22.13"
  }
}
